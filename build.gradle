subprojects {
  apply plugin: 'java'

  sourceCompatibility = JavaVersion.VERSION_1_6

  repositories {
    jcenter()
  }

  ext {
    depVersions = [
        slf4j: '1.7.12',
        jmh  : '1.11.1'
    ]
  }
}

task wrapper(type: Wrapper) {
  gradleVersion = '2.14.1'
}

